{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "conmux-app default capabilities — core event (listen/emit) for daemon-client PTY stream",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
